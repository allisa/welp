<% results.forEach(place =>  {%>
<div class="card">
  <h3>
    <%= place.name %>
  </h3>
  <img src="<%= place.image_url %>" alt="image" />
  <p>Specialties:
    <%= place.categories %>
  </p>
  <p>Rating:
    <%= place.rating %>
  </p>
  <p>Open Now:
    <%= place.hours %>
  </p>
  <address>
    <%= place.address %>
  </address>
  <p><a href="<%= place.yelp_url %>">External Link</a></p>
  <% if (!!search) { %>
  <form method="POST" action="/place">
    <input type="hidden" name="yelp_id" value="<%= place.yelp_id %>" />
    <input type="hidden" name="name" value="<%= place.name %>" />
    <input type="hidden" name="category" value="<%= place.categories %>" />
    <input type="hidden" name="rating" value="<%= place.rating %>" />
    <input type="hidden" name="address" value="<%= place.address %>" />
    <input type="hidden" name="yelp_url" value="<%= place.yelp_url %>" />
    <input type="hidden" name="image_url" value="<%= place.image_url %>" />
    <input type="hidden" name="lat" value="<%= place.lat %>" />
    <input type="hidden" name="long" value="<%= place.long %>" />
    <button id="add" value="add_id">Let's go here!</button>
    <% } else { %>
    <form method="POST" action="/place">
      <input type="hidden" name="_method" value="DELETE" />
      <input type="hidden" name="id" value="<%= place.id %>" />
      <button id="remove" value="<%= place.id %>">Remove from Saved</button>
      <% } %>
    </form>
</div>
<% }) %>